<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="rule_resource_calendar_leaves_own_or_public" model="ir.rule">
        <field name="name">Resource Calendar Leaves: Own Time Off or Public Holidays</field>
        <field name="model_id" ref="hr_holidays.model_resource_calendar_leaves"/>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="domain_force">
            ['|',
                ('holiday_id.employee_id.user_id', '=', user.id),
                ('holiday_id', '=', False)
            ]
        </field>
    </record>
</odoo>